// Test script for the generate-app API
const testCode = `
import React, { useState } from 'react';
import { Heart, Star, ShoppingCart } from 'lucide-react';

const GeneratedComponent = () => {
  const [likes, setLikes] = useState(0);
  const [rating, setRating] = useState(0);

  return (
    <div className="max-w-md mx-auto bg-white rounded-xl shadow-lg overflow-hidden">
      <div className="p-6">
        <div className="flex items-center justify-between mb-4">
          <h2 className="text-xl font-bold text-gray-900">Product Card</h2>
          <div className="flex items-center space-x-1">
            {[1, 2, 3, 4, 5].map((star) => (
              <Star
                key={star}
                className={`w-4 h-4 ${
                  star <= rating ? 'text-yellow-400 fill-current' : 'text-gray-300'
                }`}
                onClick={() => setRating(star)}
              />
            ))}
          </div>
        </div>

        <p className="text-gray-600 mb-4">
          This is a beautiful product card generated by VULK AI.
        </p>

        <div className="flex items-center justify-between">
          <div className="flex items-center space-x-2">
            <button
              onClick={() => setLikes(likes + 1)}
              className="flex items-center space-x-1 text-red-500 hover:text-red-600"
            >
              <Heart className="w-4 h-4" />
              <span>{likes}</span>
            </button>
          </div>

          <button className="flex items-center space-x-2 bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600">
            <ShoppingCart className="w-4 h-4" />
            <span>Add to Cart</span>
          </button>
        </div>
      </div>
    </div>
  );
};

export default GeneratedComponent;
`;

async function testGenerateApp() {
  try {
    console.log('🧪 Testing generate-app API...');

    const response = await fetch('http://localhost:3000/api/generate-app', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        code: testCode,
        name: 'Test Product Card',
        description: 'A beautiful product card with interactive features'
      }),
    });

    const result = await response.json();

    if (result.success) {
      console.log('✅ App generated successfully!');
      console.log('Project ID:', result.projectId);
      console.log('Project Path:', result.projectPath);
      console.log('Instructions:', result.instructions);
    } else {
      console.error('❌ Failed to generate app:', result.error);
    }
  } catch (error) {
    console.error('❌ Test failed:', error);
  }
}

// Wait a bit for the server to start, then test
setTimeout(testGenerateApp, 5000);



